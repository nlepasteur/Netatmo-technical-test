// initially I had chosen to create a variable with type value rather than directly use type as a string, to pass it it as an argument to some functions

export const UPDATE_MEASURES = "UPDATE_MEASURES";
